{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ErrorEvent",
  "description": "JSON schema for error event in life-manager application",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for error event (UUID)"
    },
    "eventType": {
      "type": "string",
      "description": "Type or code of error (e.g., NullPointerException, IllegalArgumentException)"
    },
    "service": {
      "type": "string",
      "description": "Name or identifier of service where error occurred"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Time when error occurred in ISO 8601 format"
    },
    "message": {
      "type": "string",
      "description": "Error message or brief description"
    },
    "details": {
      "type": ["object", "null"],
      "description": "Additional error data (e.g., stack trace, input parameters)"
    },
    "schemaVersion": {
      "type": "integer",
      "description": "Version of schema/format of event",
      "enum": [1]
    }
  },
  "required": ["id", "eventType", "timestamp", "message", "schemaVersion"],
  "additionalProperties": false
}

